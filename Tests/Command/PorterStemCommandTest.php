<?php
/*
namespace Manhattan\PorterStemmerBundle\Tests\Command;

use Symfony\Component\Console\Tester\CommandTester;
use Symfony\Bundle\FrameworkBundle\Console\Application;
use Symfony\Bundle\FrameworkBundle\Test\WebTestCase;

use Manhattan\PorterStemmerBundle\Command\PorterStemCommand;

class PorterStemCommandTest extends WebTestCase
{
    public function testExecute()
    {
        $kernel = $this->createKernel();
        $kernel->boot();

        $application = new Application($kernel);
        $application->add(new PorterStemCommand());

        $command = $application->find('porter:stemmer');
        $commandTester = new CommandTester($command);
        $commandTester->execute(array('command' => $command->getName(), 'name' => 'DoctrineBundle'));

        //$this->assertRegExp('/.../', $commandTester->getDisplay());

        // ...
    }
}
*/
